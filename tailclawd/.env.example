# TailClawd — local operator dashboard
# Never commit a filled copy of this file.

TAILCLAWD_HOST=127.0.0.1
TAILCLAWD_PORT=4402
# Optional bearer for POST /v1/sessions (leave empty for local-only read)
# TAILCLAWD_TOKEN=
